@if($errors->has('plan'))<div class="alert" style="border-color:#fecaca;background:#fef2f2;color:#b91c1c">{{ $errors->first('plan') }}</div>@endif
<div class="field"><label for="name">Website name</label><input id="name" name="name" value="{{ old('name', $project->name ?? '') }}" required maxlength="100"><div class="muted small">A friendly name shown in your dashboard.</div>@error('name')<div class="error">{{ $message }}</div>@enderror</div>
<div class="field"><label for="slug">Site address</label><input id="slug" name="slug" value="{{ old('slug', $project->slug ?? '') }}" required maxlength="63" pattern="[a-z0-9]+(?:-[a-z0-9]+)*"><div class="muted small">Lowercase letters, numbers, and hyphens. This will become your subdomain in Phase 3.</div>@error('slug')<div class="error">{{ $message }}</div>@enderror</div>
<div class="field"><label for="runtime">Website type</label><select id="runtime" name="runtime" required><option value="">Choose a website type</option>@foreach($runtimes as $runtime)<option value="{{ $runtime->value }}" @selected(old('runtime', isset($project) ? $project->runtime->value : '') === $runtime->value)>{{ $runtime->label() }}</option>@endforeach</select>@error('runtime')<div class="error">{{ $message }}</div>@enderror</div>
<div class="actions"><button class="button" type="submit">{{ $submitLabel }}</button><a class="button secondary" href="{{ isset($project) ? route('projects.show', $project) : route('projects.index') }}">Cancel</a></div>
